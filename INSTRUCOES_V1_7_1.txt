FACULDADE NACAR LMS - v1.7.1 HOTFIX

Correções desta versão:
1. Adicionado botão "Sair do LMS" na sidebar.
2. Logout faz signOut no Supabase e redireciona para /login.
3. Corrigido estouro visual da barra em "Cursos com mais matrículas".
4. Barra de progresso dos relatórios agora fica limitada a 100%.
5. Pequenos ajustes de overflow nos cards de relatórios.

Como aplicar:
1. Extraia este ZIP dentro de:
   C:\Users\rodri\OneDrive\Desktop\faculdade nacar

2. Ele vai substituir/criar a pasta:
   next-app

3. O pacote não inclui:
   node_modules
   .next
   .env.local

4. Depois rode:
   cd "C:\Users\rodri\OneDrive\Desktop\faculdade nacar\next-app"
   npm.cmd install
   Remove-Item -Recurse -Force .next -ErrorAction SilentlyContinue
   npm.cmd run build

5. Se passar:
   git add .
   git commit -m "v1.7.1 hotfix logout e relatorios"
   git push
